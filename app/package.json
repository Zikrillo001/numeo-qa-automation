{
  "name": "qa-demo",
  "version": "1.0.0",
  "description": "QA Automation Practice System – root workspace",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "start:backend:stable": "cd backend && CHAOS_ENABLED=false npm start"
  }
}
